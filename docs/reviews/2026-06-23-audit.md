# Post-Upgrade Integration Audit — Agentic OS v1.5.2 → v1.8.1

**Date:** 2026-06-23
**Scope:** Read-only integration audit after the v1.8.1 governance-brain upgrade (main tip `b172145`).
**Goal:** (1) verify the new safety/credential layer is wired, (2) confirm no broken references were introduced, (3) classify every validator WARN as pre-existing vs upgrade-caused.
**Verdict:** Upgrade is cleanly integrated. `validate.sh` = **pass=79 warn=10 fail=0**. Zero WARNs are upgrade-caused.

---

## 1. New safety / credential layer — WIRED ✅

v1.6–1.8 added a portable safety nucleus + credential floor. All landed and integrated, none dead:

| Artifact | Status | Wiring |
|---|---|---|
| `.agentcortex/AGENTS.safety.md` | generated | Produced from the `ACX:SAFETY-FLOOR` fenced span in `AGENTS.md` (lines 19–24) by `generate_safety_nucleus.py`. Harnesses inject it into dispatched subagents. |
| `tools/generate_safety_nucleus.py` | wired | `validate.sh:573` runs it with `--check` ("safety nucleus freshness", ADR-008). `--check` exit 0 → in sync. |
| `tools/scan_credentials.py`, `credential_floor.sh/.ps1` | present | Referenced by `validate.{sh,ps1}`, `README`, `deploy.sh`. Secrets-floor backstop. |
| `tools/validate_downstream_capabilities.py` + `templates/downstream-capabilities.example.yaml` | present | `validate.sh:565` runs it as a WARN-level gate; project has no `downstream-capabilities.yaml` → check SKIPs (optional, safe). |
| `.github/copilot-instructions.md` | present | New platform entry-point adapter. |

**Source-of-truth check:** `AGENTS.md` carries the `ACX:SAFETY-FLOOR:BEGIN/END` span; the generated nucleus is byte-in-sync (`--check` passes). Invariant text (Destructive Command Gate / Secrets Prohibition / Untrusted Tool Output / Subagent Safety Delegation) matches the span.

## 2. Broken references — NONE ✅

Validator wiring/reference gates all PASS, covering the "did the upgrade dangle a reference" risk:
`claude adapter files present` · `command sync check` · `skill metadata mirrors consistent` · `deploy_brain.{sh,ps1} → canonical deploy script` · `bootstrap/phase workflows include Recommended Skills hooks` · `L1 authority contracts` · `routing_actions contract structurally valid`.

## 3. WARN classification — 10/10 PRE-EXISTING ✅

No WARN reflects upgrade damage. v1.8.1 ships a **stricter validator** that surfaces pre-existing hygiene debt v1.5.2 never flagged (newly-surfaced ≠ newly-broken).

| WARN | Count | Provenance |
|---|---|---|
| Work logs missing ⚡ ACX sentinel in Phase Summary | 7 | Pre-existing local logs in gitignored `work/` |
| feature/arch logs missing Test Gate Results section | 6 | Pre-existing local logs |
| feature/arch logs past plan w/o ADR Coverage Check in Drift Log | 6 | Pre-existing local logs |
| Ship-PASS receipt but Current Phase ≠ ship (header not updated) | 4 | Pre-existing local logs |
| Archived logs w/ empty Phase Summary | 5 | Historical archive |
| Archived logs w/ ship receipt missing plan/implement gates | 5 | Historical governance gap |
| Shipped log still in active `work/` (archival incomplete) | 1 | Pre-existing (/ship step 3 skipped earlier) |
| `## Resume` section missing required sub-sections | 1 | Pre-existing local log |
| Stale advisory lock `main.lock.json` (owner=codex, 2026-06-14) | 1 | Pre-existing dead session, 9 days old |
| docs lifecycle / legacy domain-doc frontmatter (advisory) | ~2 | Pre-existing project docs (`docs/architecture/*`) — **newly surfaced** by v1.8.1 docs-lifecycle check |

## Recommended small optimizations (operational, not blocking)

1. **Clear the stale lock** — `python .agentcortex/tools/recover_worklog_lock.py` (or remove `main.lock.json`); it is a dead 9-day-old codex session lock. Removes 1 WARN. *(Caution: owner runs parallel codex/claude sessions — confirm no live codex session on `main` first.)*
2. **Commit the upgrade** — working tree currently holds the v1.8.1 deploy (35 real-delta files) uncommitted. Land via a dedicated `chore: upgrade agentic-os v1.5.2→v1.8.1` PR.
3. **Defer the work-log/docs hygiene WARNs** — they are local/historical and non-blocking (`fail=0`). Address opportunistically via `/govern-docs` (docs lifecycle) and Phase Summary sentinel backfill; not worth a dedicated sweep now.

## Routing

No design-level findings require routing to a canonical Domain Doc — all findings are operational hygiene / integration verification, not architectural decisions. The structured routing block is therefore omitted per the audit workflow contract (omit when no actionable findings exist).
